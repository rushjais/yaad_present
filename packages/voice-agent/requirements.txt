# [CONFIRM] pipecat extras — verify available extras for installed version
# pip install "pipecat-ai[silero,livekit,openai]"  (no deepgram needed now)
pipecat-ai[silero,livekit,openai]>=0.0.47

# Groq Whisper STT (replaces Deepgram)
groq>=0.9.0

# LiveKit token generation
# [CONFIRM] exact package name: livekit-api vs livekit-server-sdk
livekit-api>=0.6.0

# HTTP client for memory-engine and MiniMax TTS
httpx>=0.27.0

# OpenAI-compat client (TrueFoundry gateway)
openai>=1.30.0

# MP3 → PCM conversion for MiniMax TTS (requires ffmpeg system package)
pydub>=0.25.0

# Env vars
python-dotenv>=1.0.0

# For test_stt_tts.py (mic recording + playback)
sounddevice>=0.4.6
numpy>=1.24.0
