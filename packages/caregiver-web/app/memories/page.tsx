// C1 — Add memory forms: person / event / medication / story → POST /memory/write
// This is the engine of the add-fact-live demo beat. Keep forms one-click fast.
export default function MemoriesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-2">Add Memory</h1>
      <p className="text-stone-500 text-sm">Person / event / medication / story forms — coming in C1.</p>
    </div>
  );
}
