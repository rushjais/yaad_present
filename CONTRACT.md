# CONTRACT.md — FROZEN API + DATA SCHEMA

> Status: **NOT YET FROZEN.** Freeze at **Gate 0** (start of hack).
> Source of truth = `packages/memory-engine/app/schemas.py` → exported to `packages/shared/contract.openapi.yaml` → generates `packages/caregiver-web/lib/types.ts`.
> Once frozen: changes are rare and LOUD — all-hands, regenerate types, bump the version below, post in `STATUS.md`.

**Version:** v0 (pre-freeze)

See **FEATURES.md** for what each endpoint must DO, and **CLAUDE.md §3** for the full data model + endpoint list. Paste the frozen schema + endpoint shapes here at Gate 0.
